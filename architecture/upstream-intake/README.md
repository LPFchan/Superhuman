# Upstream Intake (Retired Entry Point)

The canonical upstream intake package now lives at [`../../upstream-intake/README.md`](../../upstream-intake/README.md).

Use the root package for:

- the full weekly record template
- the operator brief template
- intake method and watchlists
- carry-forward decisions
- completed `UPS-*` report artifacts

This directory remains only as a pointer so older architecture-local references do not imply a second canonical package.

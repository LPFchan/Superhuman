# Weekly Upstream Intake Report

## Review Metadata

- Review date: 2026-04-07
- Reviewer: GitHub Copilot
- Upstream window reviewed: OpenClaw release `v2026.4.5`
- Upstream refs or PRs reviewed: release notes for `v2026.4.5`, selected provider, config, plugin, routing, and memory-related surfaces
- Superhuman branch or working baseline: local Superhuman workspace, package version `2026.4.1`

## Candidate Change 1

### Candidate Change

- Title: providers and onboarding retreat from Anthropic Claude CLI subscription reuse as a default bundled path
- Upstream area: providers, onboarding, CLI backends, Anthropic
- Upstream summary: upstream removed bundled CLI text-provider backends from the core surface, removed the Anthropic Claude CLI backend from new onboarding, and documented that OpenClaw-driven Claude login traffic no longer fits the old billing assumptions
- Exact upstream feature, provider, contract, or path: Anthropic provider onboarding, `claude-cli` model path assumptions, and bundled core ownership of CLI text-provider backends
- Exact local Superhuman surface affected: the documented and typed CLI backend surface, including `agents.defaults.cliBackends`, related onboarding/product language, and the broader promise that Superhuman can reuse local tools a user already pays for
- Why it matters: this is not just an implementation cleanup; it affects whether Superhuman keeps generic local CLI fallback as part of the product story
- What this actually means in practice: upstream is backing away from treating Anthropic Claude subscription reuse as a normal built-in onboarding path, not from every local CLI path in the abstract
- Before: an operator could reasonably read the upstream product story as "Claude CLI subscription reuse is a normal bundled Anthropic path"
- After: the safer interpretation is "generic CLI backends may still exist, but Anthropic-specific subscription reuse should not be the default bundled onboarding story"
- Concrete consequence: Superhuman should keep the generic CLI seam if it wants that product capability, but should narrow Anthropic-specific claims and defaults
- What is not changing: this does not force removal of generic local CLI backend support across the entire product
- Expected end-user effect: users can still get a local-runtime option if Superhuman keeps it, but they should not be promised a fragile Anthropic subscription shortcut as the default path
- Breaking or migration risk: high if copied directly, because Superhuman already exposes CLI backends as a real typed and documented surface
- Relevance to Superhuman's current stage of development: high, because this sets product posture before launch
- Evidence source category: upstream release notes plus local docs/types inspection

### Intake Analysis

- What user or operator problem does this change solve upstream?: reduces vendor-policy risk, support burden, and misleading onboarding around Claude subscription reuse
- What assumptions from upstream do not carry over cleanly to Superhuman?: upstream is willing to narrow its product promise here; Superhuman still wants a generic local-runtime story
- Is the upstream change about policy, implementation, or both?: both
- Is this a duplicate or near-duplicate of an existing Superhuman change?: partial overlap, because Superhuman already maintains a first-class CLI backend surface
- If it overlaps an existing Superhuman implementation, whose implementation should win and why?: Superhuman's generic CLI backend shape should win for product behavior, while upstream's Anthropic-specific caution should win for vendor-sensitive positioning
- What are the main upsides of introducing this change?: clearer Anthropic positioning, lower vendor-policy risk, less support for a brittle path
- What are the main downsides, costs, or maintenance burdens?: if copied wholesale, it would erase a deliberate Superhuman product capability and push users toward API-only billing
- Does this include security or hardening work that collides with, duplicates, or weakens an existing Superhuman implementation?: partially; the upstream retreat reflects security and policy pressure, but it does not require deleting the generic local CLI architecture
- What minute compatibility details matter if this lands?: `agents.defaults.cliBackends`, plugin-owned CLI backend registration, CLI backend docs, onboarding text, and any model refs like `claude-cli/...`
- Literal user or operator scenario: a user says "I already pay for Claude locally, just use that." The product now needs to answer whether that sentence still describes a supported default Anthropic path or only a narrower expert-mode compatibility path.

### Autonomy Boundary

- Can the agent decide this autonomously?: `no`
- Why this is safe to decide autonomously, or why it is not: this changes product promise and vendor-positioning language, not just implementation
- Existing policy or prior decision that authorizes the choice: no pre-existing repo policy fully decides the product promise; the session only established the likely preferred direction
- What still requires explicit operator judgment, if anything: whether Superhuman keeps generic local CLI support as a first-class product promise and how strongly Anthropic-specific reuse is positioned

### Escalation

- Escalation required: `yes`
- Why operator input is required: this is a product-direction decision, not a routine shared-core merge choice
- Recommended decision: `adapt`
- What can proceed without approval: preserve upstream caution and stop presenting Claude subscription reuse as the default Anthropic path
- What is blocked pending approval: the final product-language and implementation posture for generic local CLI backends versus vendor-specific paths
- Re-raise by: next weekly intake if the product promise is still unresolved

### Decision

- Decision: `adapt`
- Decision owner: operator for product promise, then agent for implementation
- Ship target: pre-release docs/config posture and future upstream merge work
- Related issue, PR, or ADR: this report

### Decision Rationale

- Reason for the decision: keep generic local CLI backend support as a Superhuman-owned feature, but absorb upstream's Anthropic-specific caution instead of copying an unstable subscription workaround into the default story
- Product impact: preserves a differentiating local-runtime promise while avoiding misleading Anthropic default positioning
- Shared-core impact: shared core can still preserve generic CLI mechanisms where they are real compatibility or product seams
- Downstream `src/superhuman/` impact: docs, onboarding, and provider positioning should clearly separate generic CLI support from vendor-specific allowed paths
- Plugin ecosystem impact: preserves plugin-owned CLI backend contracts instead of deleting them wholesale
- Docs or migration impact: future docs should stop implying that Claude subscription reuse is the normal Anthropic path
- Overlap with existing Superhuman implementation: high, because Superhuman already exposes CLI backends as a typed and documented surface
- Why this decision is better than the obvious alternative: copying upstream wholesale would remove a desired product capability, while ignoring upstream entirely would miss a real vendor-policy warning
- Compatibility details to preserve during merge: `agents.defaults.cliBackends`, plugin manifest ownership of CLI backends, fallback model refs, and session reuse behavior

### Acceptance Checks

- Security implications checked: yes; the upstream retreat reflects vendor-policy and risk pressure, even though the full generic CLI seam does not need to disappear
- Correctness or bug-fix value checked: yes; it avoids a misleading Anthropic onboarding story
- Maintenance cost checked: yes; vendor-specific paths remain support-heavy
- Plugin compatibility checked: yes; generic CLI backend registration should remain stable
- Existing Superhuman implementation overlap checked: yes; local CLI backend support is already real
- Upstream-sync clarity checked: yes; upstream caution should be preserved without copying the entire product stance

### If `adapt`

- Upstream improvement being preserved: vendor-policy realism and narrower Anthropic-specific positioning
- Local adaptation approach: keep generic CLI backends, narrow Anthropic-specific claims, and preserve the generic CLI seam as downstream product behavior
- Why direct adoption is wrong for Superhuman: it would erase a deliberate product promise and overfit Superhuman to upstream's current product stance
- Compatibility layer or bridge needed: explicit documentation and policy separation between generic CLI support and vendor-specific allowed paths
- Tests that prove the adaptation: existing CLI backend coverage plus future onboarding/config tests once the wording and behavior are updated
- What Superhuman-specific product decision this adaptation depends on: operator confirmation that generic local CLI support remains part of the product promise

### Verification

- Verification status: analysis only
- Commands or checks run: local docs, types, and code inspection for CLI backends plus release-note review
- Risk level: medium
- Rollback plan if the decision later proves wrong: downgrade CLI backends to a narrower expert-oriented compatibility feature without deleting the internal seam immediately

### Operator Decision Packet

- Decision the operator must make: whether Superhuman keeps generic local CLI backend support as a first-class promise and how narrowly Anthropic-specific reuse is framed
- In simple terms, what this means: decide whether Superhuman still says "use the local tool you already pay for" and, if so, how carefully it avoids overpromising Anthropic-specific subscription reuse
- How this affects architecture or user experience: it shapes onboarding defaults, provider positioning, and whether users can rely on local CLI fallbacks instead of API-only billing
- Options: keep generic CLI support while narrowing Anthropic claims; reduce CLI support overall and follow upstream more closely
- Pros of each option: keeping generic support preserves product differentiation and local-runtime flexibility; narrowing support reduces policy and support burden
- Cons of each option: keeping vendor-sensitive paths increases support complexity; removing generic support weakens a product differentiator
- Recommended option: keep generic CLI support, but do not position Claude subscription reuse as the default Anthropic path
- Consequence of deferring the decision: docs and implementation posture stay ambiguous around vendor-specific CLI paths

### Notes for Next Intake

- Revisit date if needed: next weekly intake if the product promise is still unsettled
- Related upstream work to watch: further Anthropic provider guidance, CLI backend removals, or provider-positioning changes
- Follow-up tasks: align docs and product language once the operator confirms the CLI promise

## Candidate Change 2

### Candidate Change

- Title: remove legacy public config aliases while keeping migration and normalization compatibility
- Upstream area: config, doctor, migration
- Upstream summary: upstream removed legacy public aliases such as `talk.voiceId`, `talk.apiKey`, `agents.*.sandbox.perSession`, `browser.ssrfPolicy.allowPrivateNetwork`, and `hooks.internal.handlers` in favor of canonical public paths
- Exact upstream feature, provider, contract, or path: config schema/help output, doctor-driven rewrites, and canonical key naming
- Exact local Superhuman surface affected: docs, schema/help output, settings surfaces, and compatibility normalization for legacy keys
- Why it matters: Superhuman still exposes several stale names publicly even though it has little reason to preserve them before launch
- What this actually means in practice: old names should survive only in migration and normalization behavior, not in the public config story
- Before: users and operators can still see multiple names for the same behavior in docs or schema surfaces
- After: public docs and schema surfaces show only canonical names, while old keys still load through migration paths
- Concrete consequence: the product becomes easier to configure and less likely to accumulate future migration debt
- What is not changing: load-time compatibility and doctor rewrite support do not need to disappear immediately
- Expected end-user effect: less confusion and cleaner docs, with little practical disruption because there is no large installed base yet
- Breaking or migration risk: low to medium if compatibility normalization stays in place
- Relevance to Superhuman's current stage of development: high, because pre-release is the right time for aggressive canonicalization
- Evidence source category: local config/schema/docs inspection plus upstream release notes

### Intake Analysis

- What user or operator problem does this change solve upstream?: reduces schema sprawl and stale config guidance
- What assumptions from upstream do not carry over cleanly to Superhuman?: almost none; if anything, Superhuman has even less reason to keep legacy names prominent
- Is the upstream change about policy, implementation, or both?: both, but mostly public-surface cleanup
- Is this a duplicate or near-duplicate of an existing Superhuman change?: partial overlap, because some doctor migration support already exists locally
- If it overlaps an existing Superhuman implementation, whose implementation should win and why?: upstream canonicalization direction should win in shared config surfaces, while local migration hooks stay where useful
- What are the main upsides of introducing this change?: cleaner canonical surface, simpler docs, less long-term config ambiguity
- What are the main downsides, costs, or maintenance burdens?: a short-term cleanup sweep across docs, schema, settings, help text, and tests that still mention legacy keys
- Does this include security or hardening work that collides with, duplicates, or weakens an existing Superhuman implementation?: no; this is cleanup and migration hygiene
- What minute compatibility details matter if this lands?: keep load-time normalization and doctor rewrite behavior until retirement is deliberate, but stop presenting legacy keys in docs, UI, and schema help
- Literal user or operator scenario: a new operator opens the config docs and should see one canonical key name, not two historical names that both appear valid

### Autonomy Boundary

- Can the agent decide this autonomously?: `yes`
- Why this is safe to decide autonomously, or why it is not: this matches current policy, current product stage, and the explicit direction already given in the session
- Existing policy or prior decision that authorizes the choice: operator explicitly approved aggressive legacy-key cleanup during this chat
- What still requires explicit operator judgment, if anything: none for the cleanup direction itself

### Escalation

- Escalation required: `no`
- Why operator input is required: n/a
- Recommended decision: `accept`
- What can proceed without approval: canonicalize public config surfaces and keep compatibility only in normalization and migration layers
- What is blocked pending approval: nothing
- Re-raise by: n/a

### Decision

- Decision: `accept`
- Decision owner: agent
- Ship target: next config/docs cleanup pass
- Related issue, PR, or ADR: this report

### Decision Rationale

- Reason for the decision: Superhuman should clean out legacy aliases now, before they harden into the public contract
- Product impact: users and operators get one canonical config story instead of multiple names for the same setting
- Shared-core impact: config handling becomes simpler once aliases are limited to normalization
- Downstream `src/superhuman/` impact: docs and user-facing settings can present canonical names only
- Plugin ecosystem impact: low if normalization remains in place for imported or migrated configs
- Docs or migration impact: moderate cleanup effort, but well timed before launch
- Overlap with existing Superhuman implementation: existing doctor migrations should be retained where useful
- Why this decision is better than the obvious alternative: delaying the cleanup only increases documentation drift and future migration cost
- Compatibility details to preserve during merge: keep load-time and doctor compatibility while removing legacy names from public surfaces

### Acceptance Checks

- Security implications checked: yes; no adverse security regression was identified
- Correctness or bug-fix value checked: yes; canonical naming reduces operator error
- Maintenance cost checked: yes; it reduces long-term config sprawl at the cost of one cleanup sweep
- Plugin compatibility checked: yes; plugins are unaffected if normalization remains in place
- Existing Superhuman implementation overlap checked: yes; doctor migrations already cover some of the needed compatibility
- Upstream-sync clarity checked: yes; this should be accepted directly in principle

### If `accept`

- Merge strategy: accept upstream cleanup direction, then do local follow-up wherever docs, schema, or settings still expose old names
- Local deviations kept: load-time normalization and doctor fixes until deliberate retirement
- Required tests: targeted config, doctor, and schema coverage for migrated keys
- Follow-up cleanup: remove legacy names from help text, generated schema, settings surfaces, and docs

### Verification

- Verification status: analysis only
- Commands or checks run: local config, doctor, schema, and docs inspection
- Risk level: low
- Rollback plan if the decision later proves wrong: retain normalization longer while still keeping canonical public names

### Notes for Next Intake

- Revisit date if needed: only if a new alias-removal conflict appears
- Related upstream work to watch: further config key canonicalization or doctor rewrite changes
- Follow-up tasks: run the cleanup sweep across docs, schema, settings, and help text

## Candidate Change 3

### Candidate Change

- Title: fail-closed hook behavior and related shared-core hardening
- Upstream area: security, hooks, plugin routes, allowlists
- Upstream summary: upstream landed several hardening fixes, including fail-closed behavior when `before_tool_call` hooks crash and tighter plugin route scope fallback behavior
- Exact upstream feature, provider, contract, or path: `before_tool_call` error handling, plugin route scope fallback, and adjacent shared-core hardening surfaces
- Exact local Superhuman surface affected: local hook execution behavior, plugin route scope behavior, allowlist-related hardening, and any code paths that currently tolerate fail-open behavior
- Why it matters: Superhuman already carries adjacent hardening work, but the confirmed hook-failure path still appears weaker than upstream
- What this actually means in practice: the main issue is not abstract "more security"; it is that a broken hook can currently allow tool execution to continue when it should block
- Before: if `before_tool_call` throws, local behavior appears to fall through and allow execution with risk checks
- After: hook failure should default to blocking execution unless there is a deliberately approved exception path
- Concrete consequence: a broken safety hook stops acting like an accidental allow signal
- What is not changing: stronger local hardening that already exceeds upstream does not need to be removed
- Expected end-user effect: mostly invisible unless a hook fails, in which case the system becomes stricter and safer instead of silently permissive
- Breaking or migration risk: medium, because stricter hardening can expose hidden workflows that relied on weak fallback behavior
- Relevance to Superhuman's current stage of development: high, because security posture should be set before launch
- Evidence source category: local code inspection plus upstream release notes and related tests

### Intake Analysis

- What user or operator problem does this change solve upstream?: fail-open safety gaps and privilege leakage in sensitive runtime paths
- What assumptions from upstream do not carry over cleanly to Superhuman?: many of the same risks apply locally, but some adjacent areas are already hardened differently
- Is the upstream change about policy, implementation, or both?: both
- Is this a duplicate or near-duplicate of an existing Superhuman change?: yes for several nearby hardening areas, but not for the full hook-failure behavior
- If it overlaps an existing Superhuman implementation, whose implementation should win and why?: shared-core hardening should prefer the upstream-shaped fix unless local code is already stronger
- What are the main upsides of introducing this change?: safer defaults, fewer privilege surprises, and clearer safety policy
- What are the main downsides, costs, or maintenance burdens?: some currently tolerated workflows may fail loudly and require cleanup
- Does this include security or hardening work that collides with, duplicates, or weakens an existing Superhuman implementation?: yes; duplicates should be reconciled carefully, but weaker local paths should not be preserved accidentally
- What minute compatibility details matter if this lands?: plugin approval flows, route scope fallback tests, and any tools that currently rely on hook exceptions not blocking execution
- Literal user or operator scenario: a safety hook crashes during a sensitive tool call. The question is whether that crash should silently let the tool keep running or stop the action until the failure is understood.

### Autonomy Boundary

- Can the agent decide this autonomously?: `yes`
- Why this is safe to decide autonomously, or why it is not: shared-core hardening with no explicit contrary policy should follow upstream shape
- Existing policy or prior decision that authorizes the choice: the collision-resolution policy and the standing bias toward upstream-shaped shared-core security fixes
- What still requires explicit operator judgment, if anything: only if a later compatibility conflict would require intentionally weakening a security patch

### Escalation

- Escalation required: `no`
- Why operator input is required: n/a
- Recommended decision: `accept`
- What can proceed without approval: accept the hardening direction and patch the confirmed fail-open hook path
- What is blocked pending approval: nothing unless a new compatibility conflict appears
- Re-raise by: next intake only if a security-vs-compatibility disagreement emerges

### Decision

- Decision: `accept`
- Decision owner: agent
- Ship target: next security follow-up pass
- Related issue, PR, or ADR: this report

### Decision Rationale

- Reason for the decision: shared-core hardening should generally follow upstream unless Superhuman is already stronger, and the confirmed fail-open path is a real gap
- Product impact: safer default behavior with little user-visible downside except stricter handling when hooks fail
- Shared-core impact: brings local hook behavior closer to the upstream-safe shape
- Downstream `src/superhuman/` impact: minimal unless a product surface explicitly depended on permissive fallback
- Plugin ecosystem impact: plugin authors may need to handle hook errors more carefully
- Docs or migration impact: low
- Overlap with existing Superhuman implementation: partial duplicate category, but not complete duplicate behavior
- Why this decision is better than the obvious alternative: preserving known fail-open behavior keeps real risk without a product upside
- Compatibility details to preserve during merge: route scope tests, plugin approval semantics, and any intentionally narrow fallback scopes

### Acceptance Checks

- Security implications checked: yes; this is the primary reason to take the change
- Correctness or bug-fix value checked: yes; fail-open behavior is a correctness bug in a safety-sensitive path
- Maintenance cost checked: yes; there may be some follow-up cleanup for workflows that relied on permissive fallback
- Plugin compatibility checked: yes; plugin route scope and approval flows need regression coverage
- Existing Superhuman implementation overlap checked: yes; nearby hardening already exists locally and needs careful reconciliation
- Upstream-sync clarity checked: yes; shared-core hardening should follow upstream shape unless local behavior is stronger

### If `accept`

- Merge strategy: accept upstream-shaped hardening for shared core, then reconcile local duplicates carefully
- Local deviations kept: any already-stronger local hardening that does not conflict with the shared-core shape
- Required tests: hook-failure behavior, route scope fallback behavior, and allowlist authority checks
- Follow-up cleanup: remove duplicate local logic where upstream now covers the same protection cleanly

### Verification

- Verification status: analysis only
- Commands or checks run: local code inspection of `before_tool_call` error handling and plugin route tests
- Risk level: high enough to prioritize soon
- Rollback plan if the decision later proves wrong: temporarily add narrow compatibility handling while keeping the default secure behavior fail-closed

### Notes for Next Intake

- Revisit date if needed: only if a later hardening sync conflicts with compatibility requirements
- Related upstream work to watch: further hardening around hooks, route scopes, allowlists, or trusted-proxy behavior
- Follow-up tasks: patch the hook-failure path and compare duplicate local hardening with upstream changes

## Candidate Change 4

### Candidate Change

- Title: generic `reply_dispatch` hook and plugin-owned reply interception
- Upstream area: ACPX/runtime, reply delivery, plugin contracts
- Upstream summary: upstream added a generic reply-dispatch hook so plugins can own reply interception without core hardcoding a special path
- Exact upstream feature, provider, contract, or path: generic `reply_dispatch` hook behavior and plugin-owned interception model
- Exact local Superhuman surface affected: the public `reply-dispatch-runtime` seam, dispatcher buffering semantics, and plugin reply-delivery integration
- Why it matters: reply delivery is compatibility-sensitive, and Superhuman already exposes a public seam here
- What this actually means in practice: the architectural win is generic plugin-owned interception, but the public downstream seam should stay stable rather than churn to match upstream naming or shape exactly
- Before: Superhuman already exposes a reply-dispatch seam, but core may still carry hidden special-case assumptions
- After: shared core should move toward a generic interception model while the existing public seam remains stable
- Concrete consequence: plugins get a cleaner internal contract without breaking downstream integrations that already depend on the current exported runtime surface
- What is not changing: the existing public reply-dispatch seam does not need to be deleted or renamed just because upstream genericized its internals
- Expected end-user effect: mostly invisible, but reply routing should become less fragile and easier to reason about
- Breaking or migration risk: medium, because silent reply-delivery regressions are easy to introduce
- Relevance to Superhuman's current stage of development: medium to high, because stable shared-core contracts matter early
- Evidence source category: local export inspection plus upstream release notes and plugin adapter review

### Intake Analysis

- What user or operator problem does this change solve upstream?: removes hidden core special cases and gives plugins a formal interception path
- What assumptions from upstream do not carry over cleanly to Superhuman?: the architectural direction carries over, but the exact public seam should not churn unnecessarily
- Is the upstream change about policy, implementation, or both?: mostly implementation and contract hygiene
- Is this a duplicate or near-duplicate of an existing Superhuman change?: near-duplicate, because Superhuman already exposes a public seam in this area
- If it overlaps an existing Superhuman implementation, whose implementation should win and why?: the current public seam should win at the external contract layer, while upstream genericization should win inside shared core
- What are the main upsides of introducing this change?: clearer contracts, less hidden coupling, and better plugin compatibility
- What are the main downsides, costs, or maintenance burdens?: reply delivery is easy to break accidentally, so the sync needs careful regression coverage
- Does this include security or hardening work that collides with, duplicates, or weakens an existing Superhuman implementation?: no direct hardening collision; this is mainly contract cleanup
- What minute compatibility details matter if this lands?: `reply-dispatch-runtime` exports, dispatcher buffering semantics, plugin docs/examples, and final-delivery behavior across channels
- Literal user or operator scenario: a plugin intercepts an outgoing reply. The question is whether that interception still goes through the same stable public dispatcher seam or whether a hidden internal refactor breaks the route.

### Autonomy Boundary

- Can the agent decide this autonomously?: `yes`
- Why this is safe to decide autonomously, or why it is not: this aligns with existing compatibility policy as long as the public seam is preserved
- Existing policy or prior decision that authorizes the choice: preserve plugin-facing compatibility seams and prefer shared-core genericization over hardcoded plugin special cases
- What still requires explicit operator judgment, if anything: none unless a future sync would remove or rename the public seam

### Escalation

- Escalation required: `no`
- Why operator input is required: n/a
- Recommended decision: `adapt`
- What can proceed without approval: keep the public dispatcher seam and align shared core with a generic contract
- What is blocked pending approval: nothing
- Re-raise by: only if a future upstream sync tries to break the public reply-dispatch contract

### Decision

- Decision: `adapt`
- Decision owner: agent
- Ship target: future reply-routing sync work
- Related issue, PR, or ADR: this report

### Decision Rationale

- Reason for the decision: the upstream architectural direction is correct, but Superhuman should preserve its existing public seam instead of blindly reshaping it
- Product impact: low direct user impact, high contract-quality impact
- Shared-core impact: reduces hardcoded plugin awareness in reply flow
- Downstream `src/superhuman/` impact: minimal if the current public seam remains the entry point
- Plugin ecosystem impact: positive if compatibility is preserved carefully
- Docs or migration impact: update docs or examples only if semantics change
- Overlap with existing Superhuman implementation: high, because the seam already exists publicly
- Why this decision is better than the obvious alternative: direct churn risks breaking plugins for little gain, while ignoring upstream genericization leaves architectural debt in place
- Compatibility details to preserve during merge: public exports, buffering semantics, and channel-plugin examples

### Acceptance Checks

- Security implications checked: yes; no security regression is implied if the seam stays stable
- Correctness or bug-fix value checked: yes; generic reply interception reduces hidden routing fragility
- Maintenance cost checked: yes; the sync requires careful tests but reduces architectural debt
- Plugin compatibility checked: yes; this is the central constraint
- Existing Superhuman implementation overlap checked: yes; the seam already exists and must be preserved
- Upstream-sync clarity checked: yes; this is an `adapt`, not a blind `accept`

### If `adapt`

- Upstream improvement being preserved: generic reply interception contract
- Local adaptation approach: preserve the current public seam and move any remaining special casing behind the generic dispatcher model
- Why direct adoption is wrong for Superhuman: the public seam already exists and should not churn without need
- Compatibility layer or bridge needed: keep current exported runtime functions stable
- Tests that prove the adaptation: reply delivery and plugin integration tests
- What Superhuman-specific product decision this adaptation depends on: preserve reply-dispatch as a public compatibility surface

### Verification

- Verification status: analysis only
- Commands or checks run: local inspection of reply-dispatch exports and plugin adapter documentation
- Risk level: medium
- Rollback plan if the decision later proves wrong: keep the current public seam as an alias while refactoring internals back behind it

### Notes for Next Intake

- Revisit date if needed: only if an upstream sync proposes breaking the public seam
- Related upstream work to watch: further reply-routing, plugin interception, or ACPX runtime contract changes
- Follow-up tasks: preserve the export surface while reducing hidden special casing internally

## Candidate Change 5

### Candidate Change

- Title: dreaming, promotion, and separate synthesized-memory artifact behavior
- Upstream area: memory, dreaming, REM/deep promotion, UI
- Upstream summary: upstream added a more developed dreaming system with separate artifact storage and staged promotion behavior
- Exact upstream feature, provider, contract, or path: dreaming artifacts, staged promotion behavior, and related memory surfaces such as `dreams.md`-style outputs
- Exact local Superhuman surface affected: memory-plugin ownership boundaries, future retrieval semantics, and whether synthesized outputs become first-class memory or promoted insight
- Why it matters: Superhuman wants dreaming only if it extends memory rather than competing with it
- What this actually means in practice: dreaming should be treated as synthesis and promotion logic around memory, not as a second independent memory system with opaque rewrite power
- Before: the direction is conceptually attractive, but the trust model and ownership boundary are not fixed
- After: the likely preferred direction is plugin-owned, append-only, separate-artifact dreaming that promotes insight into memory carefully rather than overwriting memory directly
- Concrete consequence: Superhuman can borrow upstream ideas without inheriting a trust model it has not explicitly chosen
- What is not changing: there is no requirement to ship the full upstream dreaming product surface immediately
- Expected end-user effect: better long-term pattern extraction is possible, but only if the system stays auditable and trust-preserving
- Breaking or migration risk: low in the short term, because Superhuman has not committed to the upstream dreaming product shape yet
- Relevance to Superhuman's current stage of development: medium, because the architecture matters but the issue is less urgent than hardening and config cleanup
- Evidence source category: upstream release notes plus local architecture review

### Intake Analysis

- What user or operator problem does this change solve upstream?: long-term memory consolidation and promotion of durable patterns
- What assumptions from upstream do not carry over cleanly to Superhuman?: upstream UI and product surfaces for dreams are not yet part of a confirmed Superhuman product shape
- Is the upstream change about policy, implementation, or both?: both
- Is this a duplicate or near-duplicate of an existing Superhuman change?: not as shipped behavior, but it overlaps conceptually with the local memory architecture direction
- If it overlaps an existing Superhuman implementation, whose implementation should win and why?: Superhuman's memory-plugin-centered architecture should win, with upstream ideas adapted into that shape
- What are the main upsides of introducing this change?: better memory synthesis, clearer long-term recall, and background consolidation
- What are the main downsides, costs, or maintenance burdens?: if trusted too early, dreamed material can become opaque or overconfident memory
- Does this include security or hardening work that collides with, duplicates, or weakens an existing Superhuman implementation?: no direct security collision, but it materially affects trust and provenance
- What minute compatibility details matter if this lands?: where dreaming lives, artifact format, append-only versus rewrite behavior, and whether dreamed content participates in recall as first-class memory or promoted insight
- Literal user or operator scenario: the system notices a recurring pattern about the user's work. The question is whether it writes that pattern straight into trusted memory, stores it in a reviewable dream artifact first, or never promotes it without explicit review.

### Autonomy Boundary

- Can the agent decide this autonomously?: `no`
- Why this is safe to decide autonomously, or why it is not: this is product and architecture design, not routine intake work
- Existing policy or prior decision that authorizes the choice: partial direction from this session favors memory-extension over memory-duplication, but not enough to finalize the design without operator choice
- What still requires explicit operator judgment, if anything: plugin-owned versus core-owned dreaming, append-only versus rewrite behavior, and promoted-insight versus first-class-memory recall policy

### Escalation

- Escalation required: `yes`
- Why operator input is required: this changes memory product shape and trust policy
- Recommended decision: `defer`
- What can proceed without approval: keep the standing principle that dreaming extends memory rather than competing with it, and prefer separate artifact output
- What is blocked pending approval: the exact dreaming trust model and retrieval semantics
- Re-raise by: next memory-focused intake or planning cycle

### Decision

- Decision: `adapt`
- Decision owner: operator for design, then agent for implementation
- Ship target: future memory architecture work
- Related issue, PR, or ADR: this report

### Decision Rationale

- Reason for the decision: there is clear value in upstream dreaming ideas, but Superhuman needs explicit product choices before implementation shape can be locked
- Product impact: potentially large on memory trust, visibility, and user understanding
- Shared-core impact: shared core should likely provide hooks and scheduling, not own Superhuman-specific memory synthesis policy
- Downstream `src/superhuman/` impact: likely a downstream memory-plugin responsibility
- Plugin ecosystem impact: low direct impact unless memory plugin APIs expand
- Docs or migration impact: future docs will need a clear trust-model explanation
- Overlap with existing Superhuman implementation: conceptual overlap with local memory architecture goals, not yet a shipped duplicate feature
- Why this decision is better than the obvious alternative: copying upstream product surfaces now would blur ownership and trust boundaries
- Compatibility details to preserve during merge: separate artifact behavior, provenance, and staged promotion semantics

### Acceptance Checks

- Security implications checked: yes; not a classic security issue, but a trust and provenance issue was explicitly evaluated
- Correctness or bug-fix value checked: yes; staged promotion could improve memory quality if designed carefully
- Maintenance cost checked: yes; stronger automation increases review and trust-model cost
- Plugin compatibility checked: yes; likely low immediate plugin impact unless memory plugin APIs expand
- Existing Superhuman implementation overlap checked: yes; this overlaps with the intended memory architecture direction
- Upstream-sync clarity checked: yes; this remains an operator-gated adaptation, not an automatic merge choice

### If `adapt`

- Upstream improvement being preserved: staged synthesis and promotion behavior around memory
- Local adaptation approach: keep dreaming plugin-owned, separate-artifact, append-only, and promoted-insight-first until trust is proven
- Why direct adoption is wrong for Superhuman: it would import a product and trust model that Superhuman has not formally chosen
- Compatibility layer or bridge needed: clear boundary between dream artifacts and first-class memory retrieval
- Tests that prove the adaptation: future memory-promotion, provenance, and retrieval tests once the design is approved
- What Superhuman-specific product decision this adaptation depends on: whether dreamed content enters recall as promoted insight first or as first-class memory

### Verification

- Verification status: analysis only
- Commands or checks run: local architecture review plus upstream release-note inspection
- Risk level: medium
- Rollback plan if the decision later proves wrong: keep dreaming outputs in a separate artifact and downgrade or remove recall participation until the trust model is revised

### Operator Decision Packet

- Decision the operator must make: whether dreaming lives in the active memory plugin or hardcoded core, whether dreamed output is append-only, and whether dreamed content enters recall as first-class memory or promoted insight first
- In simple terms, what this means: decide whether dreaming is a careful memory assistant or a more powerful but riskier automatic memory writer
- How this affects architecture or user experience: it shapes where the logic lives, how trustworthy the outputs feel, and whether users can audit what the system believes
- Options: plugin-owned versus core-owned; append-only versus rewrite; promoted insight versus first-class memory
- Pros of each option: plugin-owned and append-only keep trust and ownership clearer; first-class memory is stronger and simpler once confidence is high
- Cons of each option: core-owned and rewrite-based systems are riskier; first-class memory too early can over-trust synthesized material
- Recommended option: plugin-owned dreaming, separate artifact, append-only and reviewable, promoted-insight layer first
- Consequence of deferring the decision: dreaming remains conceptual instead of shipping incoherent trust behavior

### Notes for Next Intake

- Revisit date if needed: next memory-focused intake or planning cycle
- Related upstream work to watch: future memory promotion, dreaming UI, retrieval semantics, and artifact-shape changes
- Follow-up tasks: turn the operator decision into a concrete memory architecture note before implementation work starts
